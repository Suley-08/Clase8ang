import { ColorStyleDirective } from './color-style.directive';

describe('ColorStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorStyleDirective();
    expect(directive).toBeTruthy();
  });
});
